// 'use strict';

// const { executeQuery } = require('../graphql/studyplan'); // Adjust path based on actual resolver location

// module.exports = {
//   async index(ctx) {
//     const { query, variables, operationName } = ctx.request.body;
//     const result = await executeQuery(query, variables, operationName);
//     ctx.body = result;
//   },
// };
